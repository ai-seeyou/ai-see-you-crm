# Category authority: Stage 0

Status: Stage 0 passes independent certification. CRM Category implementation remains unbuilt.

## Final certification

The canonical register supplies nine current core Categories across AU, NZ, SG and UK.
Production PR 66 corrects the two retired lifecycle values without changing historical evidence.
Independent verification confirms unchanged fingerprints across all thirteen protected data tables.
The CRM view, function definitions, access permissions and existing migration-history entry remain unchanged.

The final source reconciliation proves 3,010 exact governed prompt mappings.
The resulting current membership contains 13,184 property-Category pairs across 4,272 properties and 77 destination readings.
An independent SQL calculation compares every pair in both directions. It finds zero differences.
All 77 selected destination/run pairs match the independent accepted-measurement calculation.
There are zero latest-period ties, changed selected runs, excluded core CSV rows or unclassified context-free prompts.
All twenty-two independent local semantic tests pass.

The accepted rule selects the latest commercially certified, sealed, trend-valid reading per destination by period start.
Completed recommendation jobs and resolved property identities remain mandatory.
Operational run-status spelling does not replace the applicable accepted-Pulse rule.
Exact authored instrument provenance excludes local layers, contexts and retired Categories.
Historical appearances and denominator eligibility do not create current membership.

Five Hunter Valley Budget prompts retain stale PENDING_CK5 CSV status.
The pinned `docs/prompt-library/phase4_ck5_evaluation.md` records PASS with eight qualifying entities.
The governed seed loader explicitly admits that destination/theme pair after PASS.
The final calculation includes those five prompts through that exact admission evidence, not inferred readiness.

| Certified artefact | SHA-256 |
| --- | --- |
| Reconciliation script | `6fbc7ec02b564f170dd74830c5ef828d7f46bfd072ba8f2d1203c76b5572bc4e` |
| Frozen evidence JSON | `5e10b606f141e4ff261bf11dd00c52fddbd4771ca5b1a857ab3bf80c84a657c9` |
| Prompt manifest | `9c848face88d9543cdd377e8852c6b155adf68f0bf694ad20f1b55c819cc920b` |
| Membership pairs | `17f4e08afbc1364011d0a4280d6bf6d3253df131b50451b699d6faa80c761729` |

The frozen script and JSON remain in `/private/tmp/reconcile_re_prompts.py` and `/private/tmp/recommendation-environment-prompt-reconciliation.json`.
The independent comparison uses `/private/tmp/category-membership-pair-independent.mjs`.
The independent semantic tests use `/private/tmp/category-membership-semantics-review.mjs`.
Tesla implements the reconciliation. Mencius independently certifies it. The coordinator verifies the frozen hashes.

This gate certifies Category authority and membership evidence, not CRM filtering or synchronisation.
The existing 4,996-hotel import is a different, historical universe. Current Category evidence does not redefine it.
The implementation must reconcile the 77 current destination readings against that earlier 76-destination import explicitly.
All Category investigation queries remain SELECT-only.
The separately authorised lifecycle repair changes two metadata values and their automatic timestamps.
CRM receives no Production write capability or new credential.

The sections below preserve earlier audit checkpoints. This final certification supersedes their provisional counts and unresolved conclusions.

## Founder ruling after this audit

Group and Extended Stay remain retired from current core Categories.
Preserve their historical identifiers and measurement evidence unchanged.
The stale active registry flags do not override the retirement ruling.
The founder authorises governed correction of canonical registry lifecycle metadata.
The current core set contains the nine remaining governed Categories, subject to exact Stage 0 reconciliation.
Filtering implementation still waits for independent membership proof.
The sections below preserve the audit evidence and its original pause decision.

Audit date: 7 September 2026. Final report check: 02:34 UTC.

The global registry exists. Its current lifecycle flags conflict with documented instrument retirement.
Independent review confirms this conflict. The CRM does not choose a taxonomy from conflicting sources.

## Verified targets

- CRM remains on release commit `ed612497b97eb90668995fe9b395946a9d1d7ca5` during the audit.
- Production GitHub main matches local origin/main: `b06278c635bcaedca782d9a4778fc7fe2e1edac4`.
- Production repository: `ai-seeyou/tri`.
- Production project: `tnskqujimizlsmsmonor`.
- The main Production worktree has unrelated changes. The audit preserves them.
- Source conclusions use committed main, not the dirty worktree.
- The existing operator workdir `/private/tmp/tri-crm-v2` contains the verified Production project reference.
- The main Production worktree links another project. The audit does not use that link.

The operator audit uses SELECT statements only. It retrieves no database credential or service-role credential.
The CLI invocation sets `SUPABASE_DB_PASSWORD=unused-no-login-role` to prevent temporary login creation.
That value is a non-secret sentinel. Existing Management API authentication executes the approved operator reads.
This operator audit is not a new CRM runtime integration.
CRM code and deployments receive no additional access.

## Canonical registry and identifiers

The implemented registry is `public.recommendation_environments`.
Its stable identifier is `environment_id`, such as `RE-0001`.
Its display fields are `environment_name` and `display_name`.
Its semantic coordinate includes `traveller_intent`, context and scope.
It carries `evidence_tier`, `lifecycle_status`, `generation_introduced`, `generation_retired` and `updated_at`.

Relevant committed sources:

- `blueprint/03-research-standards/PIF-001-prompt-intelligence-framework.md`, section 9, defines the original eleven-intent reference instrument.
- `blueprint/03-research-standards/PIF-R-001-ratification-reconciliation.md`, sections 17 and 18, ratifies that reference instrument.
- `blueprint/03-research-standards/ACT-002-accommodation-conversation-catalogue.md`, sections 2, 6 and 17, defines permanent identifiers and registry ownership.
- ACT-002 retains draft metadata. Its implemented schema and independently ratified PIF provide separate evidence.
- `supabase/migrations/20260716140000_act002_recommendation_environments.sql` creates the registry and its lifecycle fields.
- `supabase/migrations/20260716140100_act002_prompt_template_environment_map.sql` creates the template-to-environment bridge.
- `blueprint/03-research-standards/ACT-002-catalogue-seed-v1.csv` is the historical seed, not the current lifecycle authority alone.

Live SELECT finds these eleven context-free, universal, active, Certified rows without retirement generations:

| Stable ID | Registry name | Intent coordinate | Lifecycle conflict |
| --- | --- | --- | --- |
| RE-0001 | Luxury | luxury | None identified |
| RE-0002 | Family | family | None identified |
| RE-0003 | Business | business | None identified |
| RE-0004 | Budget | budget | None identified |
| RE-0005 | Boutique | boutique | None identified |
| RE-0006 | Romantic | romantic | None identified |
| RE-0007 | Wellness | wellness | None identified |
| RE-0008 | Airport and Transit | airport_transit | None identified |
| RE-0009 | Extended Stay | extended_stay | Documented instrument retirement |
| RE-0010 | Accessible | accessible | None identified |
| RE-0011 | Group | group | Documented instrument retirement |

This table records observed registry state. It does not certify eleven current CRM filter values.
Other registry rows include experimental universal environments, occasions and contextual environments.
An active flag alone does not establish current core membership.

A later exact preflight also checks distinctness and generation metadata.
RE-0008, RE-0009, RE-0010 and RE-0011 retain `distinctness_status = 'Pending'` and `generation_introduced = 'G2'`.
Their evidence tier is Certified. Certification and distinctness are different fields.
RE-0001 through RE-0007 have Confirmed distinctness and G1 introduction.
The lifecycle correction preserves these values. It must not require invented Confirmed distinctness for the two retired rows.
Current core selection must not exclude Airport and Transit or Accessible because their distinctness remains Pending.

The exact registry selection used for the eleven-row inspection is:

```sql
SELECT environment_id, display_name, environment_name, traveller_intent,
       scope_id, lifecycle_status, evidence_tier, updated_at
FROM public.recommendation_environments
WHERE context_type = 'none'
  AND universal_or_destination_specific = 'universal'
  AND evidence_tier = 'Certified'
ORDER BY environment_id;
```

The separate lifecycle inspection confirms active state and null retirement generations.
The query exposes the conflict. It is not the approved final CRM registry predicate.

## The blocking lifecycle conflict

`docs/prompt-library/national-prompt-library-template.md` states two explicit rules:

- Rule R5 excludes retired Group and Extended Stay from every national destination.
- Section 3 lists nine core themes and records both retirements on 18 July 2026.

The latest commit touching that document is `56e999d90a927574ddb39d3e178a86a84fbdb8a7`.
`docs/analysis/9-intent-recompute-run-3ab9ecf9.md` records the executed retirement and preserves historical observations.
That report enters committed history at `84c463bfdc7deabca788a32a86b5a333b6d71edb`.
`docs/prompt-library/f1_f3_run_log.md` independently verifies retired prompt identifiers on 24 July.
Its target is `tnskqujimizlsmsmonor`, despite its historical staging label.
`docs/prompt-library/l0-l1-l2-report.md` confirms the retirement breakdown again.

Live verification finds zero active Sydney prompts for both retired intents.
However, the environment registry still marks both active and Certified, with `generation_retired = NULL`.
Both registry timestamps equal `2026-07-22 05:23:29.734285+00`.
There is one active Extended Stay template and eleven active Group templates across the inspected template table.
Those counts do not establish a global restoration ruling or current hotel measurement.
The scoped committed-source search finds no later restoration ruling.

Recommendation: retain both historical identifiers, but exclude them from current core filtering under the documented retirement.
Founder confirmation resolves the conflicting current lifecycle authority before implementation.
No registry correction or Production migration receives execution authority from this report.

## Country coverage is not the register

AU and NZ use the national prompt library. UK uses its own frozen library.
Both libraries label nine themes as core. Their contents do not independently retire or create registry identities.
Singapore has nineteen enumerated themes, including local and event themes.
Its source labels Wellness `core, C pending CK-5`.
That destination applicability qualifier does not alone change the global Category register.

Frozen source SHA-256 values:

- National library: `38281ff9098b4eded8dcfd2900407701b25a3045cdbb7380341aa05d23514165`.
- UK library: `dae63508a8afea0a1558dde06ab850d5509fa6c275ea33bb35926b75b9a05885`.
- Singapore themes: `9a194f4c5e66d7b837e01dc3375afd428559f35a2beb6951f442d3c887506c8f`.

Files: `data/prompt-library/national_prompt_library_v1.csv`, `data/prompt-library/uk_prompt_library_v1.csv`, and `data/singapore/themes.json`.
No list comes from distinct observation values.

## Identity mapping and local-layer exclusion

`sub_categories.id` identifies a destination Category, not a global Category.
`traveller_intents.id` is another identifier. Historical migration UUIDs differ from current deployed UUIDs.
The audit does not copy historical UUIDs into a new mapping.

Current active accommodation Category metadata across the four countries:

| Country | Active destination Categories | Missing intent identifier |
| --- | ---: | ---: |
| AU | 196 | 88 |
| GB | 547 | 547 |
| NZ | 84 | 44 |
| SG | 19 | 19 |
| Total | 846 | 698 |

An aggregate query restricts the next counts to completed jobs within latest sealed, commercially certified destination readings.
It requires complete, trend-valid runs. It does not yet classify global core membership.

| Country | Destinations | Measured destination Categories | Missing intent identifier |
| --- | ---: | ---: | ---: |
| AU | 11 | 193 | 88 |
| GB | 59 | 539 | 539 |
| NZ | 5 | 84 | 44 |
| SG | 1 | 19 | 19 |
| Total | 76 | 835 | 690 |

The existing template map does not cover every historical rendered prompt.
Jobs reference `prompts.id`, not `prompt_templates.id`.
The Sydney certification explicitly discloses its historical pattern-based template attribution.
Source: `supabase/migrations/20260716160000_sydney_g2_certification.sql`, lines 149 to 153.

The national seeder preserves the authored CSV coordinates, but does not persist a direct RE identifier.
Exact authoring fingerprints offer a mechanical reconciliation route. Semantic similarity does not.
A bounded Gold Coast check compares prompt-text hashes, frame and position against the exact committed national source.
It finds 53 unique exact matches from 70 active prompts: 45 core and eight local.
Seventeen prompts do not match that single source. No prompt has multiple matches in that check.
These seventeen are unresolved in this bounded check, not proven invalid or missing from all source artefacts.
MD5 serves only as a screening fingerprint. Final certification requires exact content equality or stronger content verification.
No raw prompt text leaves Production during this check.

The check confirms that generic `discovery` values agree with the authored CSV.
An earlier reviewer claim that this mismatch blocks provenance is withdrawn.
No code or decision relies on that incorrect claim.

Gold Coast and Singapore demonstrate why a null context on the Category row is insufficient.
Core-looking Category rows also contain locally narrowed prompts.
The eventual membership query must prove prompt-level core attribution and exclude context layers explicitly.
The Terminal's `is_local` display heuristic cannot define the global filter list.
Sources: DES-043, DES-046, DES-050 and `supabase/functions/terminal-read/index.ts`.

## Current-measurement semantics

The customer read path selects commercially certified, sealed Pulses.
Source: `certifiedPulseIds` in `supabase/functions/terminal-read/index.ts`, lines 431 to 438.
The Category destination then selects its latest accepted reading.
Future unfinished measurements do not erase that accepted reading.
No arbitrary date window applies.

The required evidence path is:

`governed Category ID -> proven instrument mapping -> accepted destination run -> valid recommendation observation -> resolved property ID`.

The query requires completed jobs, accepted trend-valid readings and resolved accommodation business identities.
It joins observations and jobs on both job ID and run ID.
It excludes local contexts and retired instrument coordinates using governed mappings.
Registry eligibility or a denominator entry never creates membership.
Historical appearances outside the accepted reading never create current membership.
Observation `validity_status` concerns link validity, not recommendation membership.
Aggregate scores support reconciliation. They do not replace valid observation evidence.
The exact query and real property-ID reconciliation remain uncertified until independent set comparison passes.

The founder ruling resolves the lifecycle conflict. The current source reconciliation uses nine stable registry identifiers.
These are RE-0001 through RE-0008 and RE-0010, with the names recorded above.

A later independent SELECT verifies latest accepted selection by `period_start`, not `period_end`.
It finds four accepted Pulse identifiers and no tied latest destination periods.
The final applicable rule finds 77 accepted destination readings.
The earlier complete-only check borrows a separate CRM import predicate. That check does not govern current Category measurement.
Production's customer Category and briefing paths impose no operational run-status spelling gate.
Sources at `b06278c`: `terminal-read/index.ts`, lines 548 to 555 and 624 to 632, and `briefing-compute/index.ts`, lines 306 to 314.
The `pulses.is_sealed` column definition in migration `20260807195500` establishes the same accepted-reading meaning.
It requires expected destination membership and non-false trend validity.
Commercial certification and completed recommendation jobs remain separate required checks.
The separate KM-009 per-run guard uses `completed`, not `complete`. The Category and briefing paths do not import it.
The final Category query follows the applicable accepted-Pulse contract, not that separate per-run guard.
Newer unsealed or uncertified measurements never erase an accepted reading.
Yorkshire Dales participates through accepted run `355d7227-6965-4aaa-b197-8a63817195fb`.
Earlier operational-status snapshots do not override the accepted-Pulse rule.

The existing CRM universe query intentionally accepts historical completed appearances.
It cannot supply current Category membership unchanged.
Its recommendation-summary query also omits explicit Pulse sealing.
This audit records that existing limitation. It does not change the import universe or summary query.

## Independent review and next gate

Two source investigators and an independent reviewer inspect the authority and measurement paths.
Independent review retracts the initial eleven-current-core conclusion after the later retirement evidence appears.
The original verdict identifies an unresolved lifecycle conflict, not permission to build an inferred CRM taxonomy.
Independent review of this report finds no material defect and confirms all count arithmetic.

The later founder ruling resolves that conflict without changing historical evidence.
A separate Production worktree prepares the bounded lifecycle correction for independent review.
It also protects the two retired identifiers from reactivation by the historical seed loader.
The founder separately approves the PR 66-only CI exception after a plain-language scope explanation.
An independent review finds a fail-open zero-match guard. The correction adds explicit absent, active and retired state checks.
Independent review also corrects distinctness guards to the live Pending state.
The final correction passes nine local adversarial tests, including rollback, with the existing timestamp trigger reproduced.
It locks target rows and repeats exact identity and metadata predicates in the update.
The migration SHA-256 is `dc86b55e0dbdbf5303372c5097bc7b11634d1353108256f7ee2f71efea364e50`.
The six-file review digest is `f4569edb5ef4e5abeab318ec9f9aaac049c061cff5d2cd9a6b613fb773a1ecbe`.
Production commit `418ce13` contains the reviewed correction.
[Production PR 66](https://github.com/ai-seeyou/tri/pull/66) merges at `e9f427172170956093c971a18868edecafd55584`.
Its specific CI exception does not apply to another PR.
Independent CI comparison confirms the same four baseline failures, without a new regression attributable to PR 66.
The [review receipt](https://github.com/ai-seeyou/tri/pull/66#issuecomment-5564600857) records exact hashes and checks.
The operator applies the exact reviewed SQL at `2026-09-07T04:03:11.165162Z`.
Both target rows now have retired lifecycle. Their identifiers, Certified evidence, Pending distinctness and G2 introduction remain unchanged.
The existing timestamp trigger records the correction time. It changes no related table.
The application uses the documented out-of-band data-repair process, not the automatic migration runner.
Production migration version `20260907140000` already belongs to `fix_rate_limit_increment_variable_conflict`.
The operator preserves that migration-history entry. It does not falsely record the Category repair under that version.
Independent review approves this application method under `docs/engineering/schema-reference.md`, sections 5 and 6.
Repository migration status and filename cleanup remains a separate reviewed follow-up.

The source audit reconciles 45 core prompts each for Sydney, Gold Coast and Singapore.
Each destination has five prompts for each of the nine governed Categories.
The earlier 17 unmatched Gold Coast prompts have explicit context and remain excluded from the core set.
An independent aggregate check covers 60,912 completed jobs under the accepted complete-run rule.
It finds no missing rendered prompts and no differences against their current prompt text.
The final query must still bind the actual rendered instrument and reject future drift.
Full property-ID membership reconciliation remains uncertified.
An updated independent local oracle passes twenty-two membership-semantics cases under the canonical accepted-Pulse rule.
It does not replace the final full property-pair comparison.
The frozen candidate contains 3,005 governed prompt mappings and 13,159 distinct property-Category pairs.
Every excluded prompt has explicit context or exact authored exclusion provenance.
The prompt manifest digest is `3191d7f3ec60b3c77fdac2ff9f5315d989e9ec6aadb1065fb364faa95499fb6c`.
The membership digest is `e50f67e05fd0805f89a9439d90408de1761ab9fde213e8f3d728d64c1b008010`.

After the lifecycle ruling:

1. Complete exact authoring provenance reconciliation for current accepted instrument identifiers.
2. Certify the active registry projection, exclusions, Category mappings and current-measurement rule independently.
3. Extend only the existing scoped GET contract and synchronisation path.
4. Add OR-within-Category and AND-across-dimensions filtering.
5. Derive Contact filters through the same qualifying assigned Business.
6. Verify Coverage role gaps, idempotency, removals and full-universe performance.
7. Run CI, independent review and live acceptance before contact enrichment.

No CRM code, schema, hotel data or deployment changes occur during Stage 0.
Source and membership audits use SELECT-only statements.
The separately approved registry repair changes only two lifecycle values and their automatic update timestamps.
CRM gains no Production write capability. No privilege change occurs.
No new login, credential retrieval, paid collection or outbound communication occurs.

## Issues

1. RISK. Existing CRM summary selection omits explicit sealing checks.
   Fix: Keep it separate from Category membership. Do not reuse its predicate unchanged.
2. NOT DONE. Category filters and their fifteen acceptance checks remain unimplemented.
   Fix: Implement from certified Stage 0 evidence. Keep large-scale enrichment paused.
3. RISK. The repair filename collides with an unrelated Production migration-history version.
   Fix: Preserve history and correct the repository repair status through a reviewed follow-up.
